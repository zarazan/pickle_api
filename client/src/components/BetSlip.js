import React from 'react';
import PropTypes from 'prop-types';

const BetSlip = props => {
    return (
        <section>
            Bet Slip
        </section>
    );
};

BetSlip.propTypes = {
    
};

export default BetSlip;