import React from 'react';
import PropTypes from 'prop-types';

const OpenBets = props => {
    return (
        <div>
            Open Bets
        </div>
    );
};

OpenBets.propTypes = {
    
};

export default OpenBets;