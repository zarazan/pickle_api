import * as React from 'react';
import { MemoryRouter } from 'react-router-dom';
import {cleanup, fireEvent, render} from '@testing-library/react';

import GameOdds from '../Betslip/GameOdds';

describe('<GameOdds /> spec', () => {

    // it('loads and displays page header', async () => {
    //     const {queryByLabelText, getByLabelText} = render(
    //         <MemoryRouter initialEntries={['/pool/1/schedule']}>
    //             <GameOdds/>
    //         </MemoryRouter>
    //       );

    //     expect(root.querySelector('h3').textContent).toBe('SCHEDULE & ODDS');
    // })

})