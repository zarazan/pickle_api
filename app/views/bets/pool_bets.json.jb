@bets.includes(odd: [:team, fixture: [:home_team, :away_team]]).order('fixtures.start_time').map do |bet|
  bet.as_json.merge({
    homeTeamName: bet.odd.fixture.home_team.name,
    awayTeamName: bet.odd.fixture.away_team.name,
    odd: bet.odd,
    fixture: bet.odd.fixture
  })
end
